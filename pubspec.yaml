name: jago_hitung
description: "Jago Hitung - Game Edukasi Matematika SD"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.4
  cloud_firestore: ^5.6.9
  google_sign_in: ^6.2.2
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/soal/
    - lib/data/curriculum/kelas-1/
    - lib/data/curriculum/kelas-2/
    - lib/data/curriculum/kelas-3/
    - lib/data/curriculum/kelas-4/
    - lib/data/curriculum/kelas-5/
    - lib/data/curriculum/kelas-6/
